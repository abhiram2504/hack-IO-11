import React from "react";

function Statistics() {
    return (
        <div className="statistics">
            {/* Visits, Bounce Rate, Pages per visit, etc. */}
            Statistics
        </div>
    );
}

export default Statistics;